@model Chatly.Models.ChatRoomViewModel
@{
    ViewBag.Title = "Chat";
}
<h1 class="text-center mt-2">InfoServ Chat</h1>
<div class="row">
    <div class="col-md-12">
        <div class="d-flex justify-content-center">
            <form id="frmChat" class="w-100" runat="server">
                <div class="row">
                    <div class="col-md-9">
                        <label for="txbUserName">Username:</label>
                        <div class="form-group d-flex">
                            <input type="text" class="form-control" id="txbUserName" placeholder="name" value=@Model.UserName>
                            <input id="btnJoin" type="button" class="btn btn-primary" value="Join" />
                        </div>
                    </div>

                    <div class="col-md-3">
                        @Html.LabelFor(m => m.CodeId)
                        @Html.DropDownListFor(m => m.CodeId, new SelectList(Model.Codes, "Id", "Code"), new { @class = "form-control", id = "ddl1" })
                        @Html.ValidationMessageFor(m => m.CodeId)
                    </div>
                </div>
                <div class="row mt-1 ">
                    <div class="col-md-9">
                        <textarea id="txaChat" class="w-100" rows="20" readonly="readonly"></textarea>
                    </div>
                    <div class="col-md-3">
                        <textarea id="txaUsers" name="S2" class="w-100" rows="20" readonly="readonly" ></textarea>
                    </div>
                </div>
                <div class="control-form mt-3 d-md-none d-flex">
                    <div class="col-md-12">
                        <input id="txbMessageTwo" type="text" class="form-control" />
                        <input id="btnSendTwo" type="button" class="form-control mt-1 btn btn-primary" value="Send" />
                    </div>
                </div>
                <div class="control-form d-sm-flex d-none mt-3">
                    <div class="col-md-9">
                        <input id="txbMessageOne" type="text" class="form-control" />
                    </div>
                    <div class="col-md-3">
                        <input id="btnSendOne" type="button" class="form-control mt-1 btn btn-primary" value="Send" />
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
@section scripts {
    <script src="Scripts/jquery-1.10.2.min.js"></script>
    <!-- SignalR Lib -->
    <script src="~/Scripts/jquery.signalR-2.4.1.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!-- Chat Room Javascript (SignalR)-->
    <script src="~/Scripts/mpw-chat.js"></script>

    <script>
        $(document).ready(function () {
            //$("#btnJoin").click();

            $('#txbMessage').on("keypress", function (e) {
                var key = e.charCode ? e.charCode : e.keyCode ? e.keyCode : 0;
                if (key == 13) {
                    $("#btnSend").click();
                }
            });
            $("#ddl1").change(function () {
                $("#btnJoin").click();
            });
        });
    </script>
}